[project]
name = "bot"
version = "0.1.0"
description = "Telegram bot for ToDo List service"
requires-python = ">=3.13"
dependencies = [
    "aiogram>=3.15",
    "aiogram-dialog>=2.2",
    "httpx>=0.28",
    "pydantic-settings>=2.6",
]
